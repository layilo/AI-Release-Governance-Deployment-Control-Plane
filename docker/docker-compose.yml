version: "3.9"
services:
  control-plane:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    environment:
      ARCP_PROFILE: local-demo
      ARCP_MODE: mock
    volumes:
      - ../state:/app/state
      - ../reports:/app/reports
